# copy to .env and fill in. .env is gitignored.

# HuggingFace token: avoids download rate limits, required for gated models (Llama).
HF_TOKEN=hf_...

# OpenRouter API key: used for hosted model inference.
OPENROUTER_API_KEY=sk-or-v1-...
